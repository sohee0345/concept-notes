# 전처리 파이프라인 모듈화

## 1. 개념

반복되는 결측치 처리, 인코딩, 스케일링을 함수와 모듈로 분리하면 여러 실험에 같은 규칙을 재사용하고 단계별 결과를 검증하기 쉽다.

> **핵심:** 전처리 단계를 함수로 분리하고 train 학습·test 변환 규칙을 함수 안에서도 유지한다.

------------------------------------------------------------------------

## 2. 구조

```text
preprocessing.py
  ├─ feature_target_split()
  ├─ transform_encoding()
  └─ transform_scaling()
```

```python
x_train_scaled, x_test_scaled = transform_scaling(
    scaler,
    x_train_number,
    x_test_number,
)
```

------------------------------------------------------------------------

## 3. 검증

- train Feature와 Target의 행 수가 같은지 확인한다.
- train과 test의 Feature 컬럼 수와 순서가 같은지 확인한다.
- 결합 전 DataFrame 인덱스를 맞춘다.

------------------------------------------------------------------------

## 🟦 핵심 정리

### ⭐ 한 줄 정리

> **전처리 코드를 모듈화하면 동일한 변환 순서와 검증 규칙을 반복해서 적용할 수 있다.**

### 🔖 복습할 내용

- [ ] 전처리 단계를 함수로 나누기
- [ ] 함수별 입력과 출력 정의하기
- [ ] 결합 전 shape과 인덱스 검증하기

