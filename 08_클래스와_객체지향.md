# 클래스와 객체지향

## 1. 개념
클래스는 데이터와 기능을 하나로 묶어 객체를 만들기 위한 설계도다.

> **핵심:** 클래스에서 인스턴스를 만들고, 각 인스턴스는 속성과 메소드를 가진다.

---

## 2. 쉽게 이해하기
```text
클래스 = 설계도
   ↓
인스턴스 = 설계도로 만든 실제 객체
```

---

## 3. 사용 방법
```python
class Person:
    species = "Human"

    def __init__(self, name):
        self.name = name

    def introduce(self):
        return self.name

person = Person("홍길동")
```

- `class` → 클래스 정의
- instance → 클래스로 생성된 객체
- `self` → 현재 인스턴스 자신
- `__init__()` → 생성 시 초기값 설정
- 클래스 속성 → 모든 인스턴스가 공유
- 인스턴스 속성 → 객체마다 따로 저장
- 상속 → 부모 클래스 기능 물려받기
- 오버라이딩 → 부모 메소드 재정의
- 매직 메소드 → 특정 상황에 자동 호출
- 싱글톤 → 인스턴스를 하나만 유지하는 방식

---

## 4. 예제
```python
class Animal:
    def speak(self):
        return "소리"

class Dog(Animal):
    def speak(self):
        return "멍멍"

print(Dog().speak())
```

실행 결과:
```text
멍멍
```

---

## 5. 헷갈리는 개념 비교
|구분|클래스 속성|인스턴스 속성|
|---|---|---|
|공유 여부|모든 인스턴스가 공유|각 인스턴스별로 보유|
|예시|`species`|`self.name`|

---

## 🟦 핵심 정리

### 💡 주요 개념
class, instance, self, `__init__`, 상속, 오버라이딩, 매직 메소드

### 💻 주요 코드
|코드|의미|
|---|---|
|`class Name:`|클래스 정의|
|`self.x = x`|인스턴스 속성 저장|
|`class Child(Parent)`|상속|

### ⭐ 한 줄 정리
> **클래스는 관련된 데이터와 기능을 하나의 객체 단위로 묶어 관리하는 방법이다.**

### 🔖 복습할 내용
- [ ] 클래스와 인스턴스 차이
- [ ] self의 의미
- [ ] 상속과 오버라이딩
